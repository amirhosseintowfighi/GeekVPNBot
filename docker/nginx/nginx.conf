user nginx;
worker_processes auto;
# Raised because `worker_connections` below is meaningless without it: a
# proxied request holds two descriptors, one to the client and one to the
# upstream, and the container's default soft limit of 1024 caps nginx far
# below what it thinks it is configured for. Nginx says so at every start
# - "worker_connections exceed open file resource limit" - and then hits
# the real ceiling under the burst of chunk requests a Next.js page makes.
worker_rlimit_nofile 8192;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 2048;
    multi_accept on;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # JSON access log so it lands in the same pipeline as the app logs and
    # carries the same correlation id.
    log_format json_combined escape=json
      '{"ts":"$time_iso8601","remote_addr":"$remote_addr","method":"$request_method",'
      '"path":"$uri","status":$status,"bytes":$body_bytes_sent,'
      '"duration_ms":$request_time,"upstream_ms":"$upstream_response_time",'
      '"referer":"$http_referer","user_agent":"$http_user_agent",'
      '"correlation_id":"$request_id"}';

    access_log /var/log/nginx/access.log json_combined;

    # Docker's embedded DNS. Required because every proxy_pass below names its
    # target through a variable, which is what defers name resolution to
    # request time. With a literal hostname nginx resolves at *config load* and
    # refuses to start if even one backend is absent - so a stopped blue/green
    # colour, or a front-end container that does not exist in this stack at
    # all, took the whole edge down and with it the API that was running fine.
    # 300s, not 10s. A container's address changes only when the container
    # is recreated, and every path that recreates one here also reloads or
    # restarts nginx, which starts workers with empty caches. A ten-second
    # TTL bought no freshness and put a DNS round trip in the way of the
    # first request after every quiet minute.
    resolver 127.0.0.11 ipv6=off valid=300s;
    resolver_timeout 5s;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    server_tokens off;
    client_max_body_size 8m;
    client_body_timeout 15s;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types application/json application/javascript text/css text/plain application/xml;

    # Shared rate-limit zones. Applied per-location in conf.d.
    limit_req_zone $binary_remote_addr zone=api_zone:10m rate=20r/s;
    limit_req_zone $binary_remote_addr zone=webhook_zone:10m rate=50r/s;
    # Admin surfaces get their own zone. Sharing api_zone would let ordinary
    # customer traffic exhaust the budget an operator needs during an incident,
    # which is exactly when the panel must stay usable.
    limit_req_zone $binary_remote_addr zone=admin_zone:10m rate=30r/s;

    # Connection cap alongside the request-rate caps. A rate limit alone does not
    # stop a slow-loris client: it opens sockets without completing requests, so
    # it never trips a per-second counter.
    limit_conn_zone $binary_remote_addr zone=conn_zone:10m;
    limit_conn conn_zone 60;

    # Refusals answer 429, not the default 503. A 503 tells the client (and any
    # monitoring in front of us) that the service is down, when in fact it is
    # working correctly and declining this particular caller.
    limit_req_status 429;
    limit_conn_status 429;

    proxy_http_version 1.1;
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Request-ID      $request_id;

    # Named, not globbed. Two files in conf.d are fragments the template
    # includes itself, inside a server or location block, and a glob pulled
    # them in here at http level as well:
    #
    #   active-api.conf   `set` is not allowed in http context - fatal, and it
    #                     only surfaced once the upstream blocks that used to
    #                     fail earlier in the same parse were removed.
    #   admin-allow.conf  `allow`/`deny` ARE allowed here, which is worse: with
    #                     ADMIN_ALLOW_CIDRS set, its `deny all` would apply to
    #                     every server block and lock the whole edge to the
    #                     admin networks. Silent, and only for operators who
    #                     configured the allowlist.
    #
    # Anything genuinely http-level goes on this list explicitly. Upstreams
    # first, so they are defined before geekvpn.conf refers to them.
    include /etc/nginx/conf.d/00-upstreams.conf;
    # Written by the entrypoint, always - empty when a front-end is absent,
    # because an `include` of a missing file is fatal. It has to be named here
    # for the same reason this list is explicit at all: the pools file was
    # being generated correctly and read by nothing, so every front-end request
    # treated the pool name as a hostname and 502ed on the lookup.
    include /etc/nginx/conf.d/10-frontend-pools.conf;
    include /etc/nginx/conf.d/geekvpn.conf;
}
