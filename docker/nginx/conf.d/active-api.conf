# Which colour currently serves traffic. Rewritten by scripts/deploy.sh, then
# `nginx -s reload`.
#
# Kept to a single line on purpose: a deploy that has to template an entire
# config file is a deploy that can produce an invalid one at three in the morning.
set $active_api api_blue;
