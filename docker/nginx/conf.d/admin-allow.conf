# Edge allowlist for admin surfaces. Rendered at container start by
# docker/nginx/entrypoint.sh from ADMIN_ALLOW_CIDRS.
#
# Empty by default, so a fresh install is not locked out before the operator has
# decided what their office address is. The application-level allowlist is the
# one that must be configured; this is defence in depth on top of it.
